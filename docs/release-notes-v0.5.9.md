# Cleo v0.5.9

- 电脑操作改为直接输入 `/computeruse 操作需求`，移除 on/off 交互；仍使用当前会话所选模型。
- Windows-MCP 按需启动，没有配置文件或旧配置为关闭时也可调用；不重写旧配置。
- 修复 Claude 进化会话切换至无人值守模式的启动参数缺失；通过重新连接应用权限，保留原生会话历史。
- 修复停止 Claude 任务后复用未读完的流，导致下一轮读到旧工具拒绝或结束消息的问题。
- 普通任务继续遵守所选权限模式；进化构建检查、持久数据兼容和受保护的版本恢复流程保持有效。
