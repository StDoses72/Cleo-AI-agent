# Cleo v0.5.11

- 右侧「电脑」升级为独立 Linux 桌面，实时显示浏览器和常用应用，不再镜像主机屏幕。
- `/computeruse` 默认操作该桌面，保留当前模型选择，不再抢占主机窗口和键鼠。
- 点击画面即可接管，支持鼠标、滚动、键盘和中文输入；人工接管期间暂停 AI 操作及截图，交回后继续。
- 增加观看暂停、桌面放大和关闭桌面；浏览器资料保留在专用 Docker 数据卷。
- 独立桌面需要 Docker Desktop，首次自动构建运行环境；故障时明确报告，不回退操作主机。
