# Cleo v0.5.10

- 新增右侧「电脑」面板：电脑操作时自动显示真实桌面，支持浏览器与其他软件、多显示器切换、暂停预览和停止任务。
- 显示最近的电脑操作与等待确认状态；预览关闭或窗口隐藏时停止采集，画面仅保留在内存。
- 修复 Claude 截图工具消息超过默认 1 MiB 接收上限导致任务中断的问题，统一采用有上限的 32 MiB 缓冲。
- 异常中断后的 Claude 连接会重新建立并保留历史，避免后续任务读取旧流残留。
- 沿用 `/computeruse 操作需求` 直接调用方式，无需 on/off，也不切换当前模型。
