# UI/UX 与相关 issues 修复进度

范围：完成 UI_UX_AUDIT_v0.4.8.md 中 U01–U23，并处理已识别的 UI/UX issues #29、#30、#32、#33、#34、#37、#38、#52、#57、#58。#10 只有标题，需要核对 v0.4.8 的同会话模型切换与其原意；分支申请和 #11 不作为界面缺陷处理。

完成条件：每项有实际实现与对应验证；全套检查、深浅主题、窄窗口、键盘/中文输入、失败恢复和必要后端契约通过；临时文件清理；最终重审；不能仅凭测试通过宣称所有问题完成。

## 基线

- 分支：codex/uiux-issues-v0.4.8。
- 0666b42：将 v0.4.8 产品与测试变化整合到 main 的历史上，保留 main 文档、AGENTS.md 和 data 示例。尚未推送或改变远端 main。
- d442ac6：第一批模态、草稿、错误反馈与自动记忆刷新修复。
- 审查报告仍记录修改前状态，以固定 v0.4.8 源码链接作为基线。
- 视觉方向：克制、正文优先、简洁可读；内容顺序为工作内容、当前状态、必要操作、按需详情。动效只用于状态变化、展开收起和焦点反馈。

## 状态

| 范围 | 状态 | 验证/剩余要求 |
| --- | --- | --- |
| U01 / #37 焦点与快捷键 | 已实现首批，待整体视觉验收 | native modal、Tab 循环、嵌套 Escape、屏蔽底层审批与全局快捷键；新增浏览器回归通过 |
| U02 草稿 | 已实现并回归 | 指令与连接向导切页保留；指令关闭重开不被后台读取覆盖；记忆模型设置跨页保留草稿，保存前不显示已生效 |
| U03 失败恢复 | 已实现并回归，待最终综合验收 | 模型/指令/任务读取错误与重试、启动重试；进化首次读取失败可重试，旧读取不覆盖新事件；状态重读与业务操作重试分开，避免重新提交已接受的操作 |
| U13 / #29 记忆刷新 | 已实现，待最终综合验证 | 独立 load_memory 接口；进入页面、窗口返回、可见页定时、回合完成与整理结束刷新；合并并发请求、忽略旧响应；失败保留数据且可重试 |
| U04–U07 / #52 / #58 | 核心实现并验证，待最终综合验收 | 无分页按钮；短页面自动补页；加载状态不挤动正文；按帧合并滚动与减少布局读取；万条历史锚点通过；正文对齐的单一活动状态；等待审批/回答停动画；过程改轻量行；长正文 Markdown 自动续载与失败重试 |
| U08–U10 / U14–U16 / U23 | 已实现，待最终综合验收 | 正文14/控件13/辅助12字号与统一字重；删装饰眉题和机制说明；记忆页以列表为主、对话标题识别来源、详情按修订自动更新；未知用量不显示0%；记忆可展开预览；移除假连接绿灯和空菜单 |
| U11–U12 / #32 | 已实现并回归，未作macOS真机验证 | 账号连接和详情自动探测；明确安装入口；单次选模型、保存后直接返回；手动目录模式不伪装已验证；GUI进程补查用户安装/Homebrew路径，保留明确配置优先级及原PATH顺序 |
| U17–U19 | 已实现并回归，待最终综合验收 | 普通聊天改进入口移入更多菜单；进化顶部只保留状态和可用动作，GitHub 授权进入提交流程；验收目标只保留一份清单，证据/反馈/历史按需展开，确认、取消、调整与中断恢复保留；分支、PR、发布版本自动读取；输入不被刷新覆盖，失败才重试；提交/修复/发布仍明确触发 |
| U20 更新 | 已实现并回归，待最终综合验收 | 进入与返回窗口时按过期时间自动检查；隐藏/关闭页面暂停，合并同时请求、失败退避；保留选定版本和已下载包；只保留下载、重启、失败重试，历史版本及依赖信息折叠 |
| U21 发布 | 已实现并回归，待最终综合验收 | 打开表单、切换 PR/账号时自动核对发布权限和合并源码；迟到结果不覆盖新来源，表单保留输入；隐藏时暂停、失败重试；发布仍明确触发；自动预检不阻止对话、不覆盖构建诊断与人工操作的重试入口 |
| U22 / #33 Steer | 草稿可编辑、重复切换提示已处理；Steer 待实现 | 原生能力与降级、请求去重、投递状态、持久化、取消竞态；不能将可编辑草稿当作 Steer 已完成 |
| #57 并行任务 | 已实现并回归，待最终综合验收 | 每任务运行编号与锁；审批、提问草稿和提交、停止独立；晚到读取不覆盖流式输出；刷新后自动恢复和同步运行、审批与提问，读取失败保留运行状态；共享仓库并发时禁用无法归属单任务的回退 |
| #34 / #38 | 已实现并专项回归，待最终综合验收 | Codex 独立访问/审批模式；Claude 原生审批与 ACP 请求选项；保存/恢复/下轮生效；原生审查记录与人工决定可回看；重复响应与取消竞态；Windows 真实沙箱写入及恢复限制通过 |
| #30 耗时 | 待实现 | 普通回复及 DreamAgent 实际阶段计时、失败/重试/恢复、持久化与显示 |

## 已运行验证

- 新增 ui/tests/uiux-regressions.smoke.mjs：先复现旧版失败；23项全量场景及新增记忆模型草稿场景分别通过，共24项。覆盖自动连接、迟到目录响应、选择失败、真实状态、模态、草稿、全文阅读和记忆刷新。
- tests/desktop/test_service.py 与客户端发现、Claude CLI、MCP、登录相关测试：51项通过；包括来源标题、GUI缺失PATH、显式路径优先级和不改变进程PATH。
- model-connection-scope：7项通过，含不把未提供模型目录伪装成认证成功或失败；时间线缓存与harness IPC在前一批通过，最终会再跑。
- npm run typecheck --prefix ui、Vite production build、修改 Python 文件的 Ruff 检查通过；构建仍有已有的大 chunk 提示。
- smoke-history：万条历史、双向分页、加载失败、延迟图片、阅读锚点、任务切换与提问通过。
- smoke-layout：深浅主题、宽/窄窗口与 150% 缩放共 8 个场景通过；已查看宽窗口及窄窗口缩放截图。
- smoke-ux：11 个场景通过。修正了该测试复用旧 dist/默认用户目录、缺失 desktop bridge 方法的问题，现在每次在临时目录构建并使用隔离用户数据，finally 清理。
- 综合smoke：在隔离目录构建后通过（包括全部设置页、模型、记忆、附件、审批、删除、紧凑窗口），控制台无错误。测试文案断言随可见文案更新，运行参数仍按实际选项value校验。
- 并行专项：concurrent-runs 和 running-recovery 浏览器场景通过，覆盖同名审批/问题、并发回答、旧快照、晚到任务读取、恢复失败与自动重试、运行编号过期的停止回复、后台完成不改变当前草稿和阅读位置；无浏览器异常。晚到任务读取先复现失败，再修复通过。
- 并行改动后的 smoke-ux 11项、smoke-history、进化/普通任务隔离7项、uiux回归24项通过；已查看双任务运行界面。
- 后端 desktop + cancellation/questions 首轮177项通过、6项失败；修复检查顺序与测试夹具后，相关41项和5项分别通过，原失败项全部覆盖。Electron backend 76项通过、2项平台跳过；IPC 3项通过；TypeScript、生产构建、修改Python的Ruff通过。
- 更新与发布：新增 update-settings 浏览器回归通过；smoke-program-updates 的6组流程（含深浅主题、窄窗口及缩放）通过；发布表单回归增加自动只读预检、来源切换迟到错误、输入保留与折叠暂停，全部通过。已查看更新页及窄屏发布表单截图。
- 发布后端50项、进化管理/失败重试/更新/GitHub相关51项、通用版本与验收35项通过（1项平台跳过）；IPC 4项通过；选版17项中16项通过、1项需要旧安装包的兼容测试跳过。界面回归24项、TypeScript与生产构建通过。预检测试确认不产生远端写入；未连接真实发布账号或发布版本。
- 进化界面：smoke:evolution-ui 的总览/贡献、合并预检、交互验收、应用与保存约束通过；准备流程9个场景通过，包括无重复清单、纯问答、失败重试、调整目标、恢复已提交但未完成的修改；取消验收不增加通过记录。无源码改动也会自动比较当前版本，仍等待明确人工确认。
- 完整 test:evolution：271项通过、3项跳过；扩展的实际IPC入口测试通过，确认只读预检期间普通任务可运行，写操作、进化编辑和版本事务的互斥仍生效。PR提交版本未变化时复用不可变提交的本地比对，CI与权限仍重新读取。
- 实际Electron smoke:evolution 与7个跨视图隔离场景通过；新增目标焦点、首次状态读取失败的恢复、已检查候选版本的应用入口均有回归。修正旧smoke混用renderer mock与临时IPC任务的夹具，改为从启动即使用隔离的IPC数据。深浅主题、窄窗口和150%缩放8个布局场景通过，已查看实际截图；严格检查相关TSX测试夹具与生产构建通过。
- 最终全套测试、lint、production build 与整体视觉验证尚未完成。

## 权限设置首批与暂停点

- 设置明确作用于当前任务；Codex 的只读、工作区可写、完全访问与人工审批、原生自动审查、拒绝审批请求分别选择。自动审查可能允许或拒绝，拒绝审批请求不表示自动允许。外部服务授权仍独立。
- 修复桌面创建/恢复/切换时将 `auto_review` 改成人工审批的问题；保留已选策略，人工审批通道可供后续切换使用。运行期间只保存待生效更改，不解决或改变正在等待的审批；下一轮发送前应用，失败阻止该轮启动并保留待生效值。可取消更改，迟到响应不覆盖新设置，不跨任务或服务套用权限。
- 本批验证：desktop service/harness/skills 70 项通过；原生适配层、会话恢复、取消、提问与进化约束相关 60 项及 9 个子测试通过。新增浏览器权限回归覆盖独立设置、失败恢复、待生效值、取消更改、迟到响应、任务隔离、未支持选项和深浅主题/窄窗口；已查看截图。通用 UI/UX 24 项、并行任务与运行恢复两组浏览器回归通过。TypeScript、修改 Python 的 Ruff、生产构建通过，保留已有的大 chunk 提示。
- 本批未调用收费模型、修改真实用户目录的访问权限或关闭远端 issue。SDK 参数映射与保存恢复有测试，但真实运行时的沙箱边界、受管理策略限制及原生自动审查记录尚未验证；不能把这一批称为 #34/#38 全部解决。Claude/ACP 保留自身配置并显示暂不提供编辑控件，不套用 Codex 模式。
- 依据：[Codex App Server 官方文档](https://learn.chatgpt.com/docs/app-server)及本机 `codex-cli 0.154.0-alpha.6.2` 生成的协议 schema、已安装 SDK 的原生策略映射。
- 用户要求这批结束后停下，等回家再继续。下一步先补完 #34/#38 的剩余部分，再处理 #33 Steer/U22、#30 计时及最终整体验收；此处不将整个 goal 标为完成。

## 恢复后：审批记录与原生权限验证

- 用户已回家并明确要求继续。补齐 Codex 原生自动审查记录：允许、拒绝、超时、中断分别展示，来源与原因按需展开；人工审批保留请求和决定。旧历史索引按投影版本重建，不改写权威会话日志。审批元数据不作为新的模型指令或记忆正文。
- 两个 Codex 竞态先复现再修复：记录决定期间的重复提交和取消曾覆盖第一个决定；现在原决定固定，重复请求明确拒绝，取消释放等待，界面断线不改变已作出的决定。Claude/ACP 共用异步审批生命周期，保持各自的原生返回值与作用范围。
- Claude 提供原生 default、acceptEdits、auto、dontAsk、bypassPermissions、plan 选项；需要确认的调用进入人工审批。自动允许的成功工具在详情内记录后端策略，原生拒绝单独记录。修正 Claude 工具结果内容/失败状态与流式工具标识投影。ACP 显示原生允许/拒绝选项名，并支持人工、自动允许与自动拒绝，缺少对应选项或运行结束时取消，不伪造允许。两者都不提供虚假的 Codex 沙箱选项。
- Codex 人工模式在创建、恢复和 fork 时直接传入原生 user/on-request，不再先设 deny_all；覆盖三种访问范围的九个生命周期组合。修改权限前读取原生管理策略，不允许的范围或审批方式不保存。
- 原生验证：使用安装的 Codex App Server `command/exec` 与产品同一沙箱映射，在隔离目录进行六轮、十二次写入：完全访问、只读、工作区写入、再次完全访问、恢复工作区写入、恢复只读。工作区内外结果及文件存在性符合预期。使用 Windows unelevated 沙箱，无模型调用、无真实用户文件写入、无系统沙箱安装。Windows pytest 私有 ACL 会额外拒绝合法工作区写入，测试改用本任务目录下继承常规 ACL 的独立目录；测试运行时关闭无关插件同步，正常运行后自动清理。
- 验证：相关 desktop/投影/历史/取消/提问/后端回归 134 项通过；新增/相关审批生命周期、原生模式、管理策略和恢复回归最终 64 项通过；真实沙箱测试通过。权限浏览器回归含 Claude/ACP 控件、自动审查历史、失败/下轮生效/任务隔离；并行与恢复两组浏览器回归、TypeScript、Ruff 和生产构建通过。已查看审批历史及设置深浅主题截图。构建仍有原有的大 chunk 提示。
- 补充来源：[Claude 权限契约](https://code.claude.com/docs/en/agent-sdk/permissions)、[Claude 人工输入契约](https://code.claude.com/docs/en/agent-sdk/user-input)、[ACP 原生审批选项](https://agentclientprotocol.com/protocol/v1/tool-calls)。尚未在真实 Claude/ACP 账号上发起收费模型任务；回调、原生返回类型和参数/持久化路径使用集成 SDK 类型与协议夹具验证。
- 最终视觉检查需复查 760px 三栏同时展开时正文过窄的布局；当前权限页在窄窗口可读，但正文与检查器同时展示仍需改进。

并行任务共用同一个工作目录时，文件仍然共享；这一实现不自动创建工作树。重叠运行不会生成混合改动的单任务撤销记录；有任务写入时，工作区重置和回退会明确阻止。程序退出先取消并等待运行清理，再关闭服务。

客户端路径依据：[Claude官方安装与路径排查](https://code.claude.com/docs/en/troubleshoot-install)、[Claude安装说明](https://code.claude.com/docs/en/setup)、[Homebrew默认安装位置](https://docs.brew.sh/Installation)。测试使用隔离文件夹中的假客户端，仅检查解析，不启动真实登录或收费模型请求。

## 临时资源

统一目录：C:\Users\steph\AppData\Local\Temp\cleo-uiux-fixes-20260916。基线导入 patch 已删除；测试将子目录放在此处并通过 finally 清理。最终完成前须删除整个任务临时目录并复核 git status。

并行任务阶段清理：普通临时文件、浏览器文件、截图、日志、缓存与空目录已删除。上述目录内剩余52个测试仓库只读Git对象，位于 pytest-concurrent-safety、pytest-concurrent-safety-recheck、pytest-desktop-all、pytest-final-concurrency、pytest-service-concurrency 子目录；自动审批拒绝带 `-Force` 的删除，普通删除因只读属性失败。未声称清理全部完成，后续完成验收时需处理该明确残留。

更新/发布阶段新增的 updates 子目录（浏览器测试、截图、缓存）已清理；复核仍只有上述52个既有只读对象。

进化/验收/贡献阶段的 evolution-ui 子目录（隔离运行目录、日志、截图与缓存）已清理；复核仍仅剩此前被自动审批拦截的52个只读对象。此阶段没有创建远端 PR、分支申请或发布版本。

权限阶段：协议 schema、浏览器截图、构建、缓存和其他普通临时文件已清理。新增23个只读Git测试对象残留在 C:\Users\steph\AppData\Local\Temp\cleo-uiux-fixes-20260916\permissions\backend（12个）与 C:\Users\steph\AppData\Local\Temp\cleo-uiux-fixes-20260916\permissions\backend-recheck（11个）。连同前述52个共75个；遵守此前自动审批对强制删除的拒绝，未重试或绕过。此限制只影响临时测试对象清理，不影响已提交代码；未声称临时目录已全部清空。

权限补齐阶段：普通临时文件、截图、构建与缓存已清理；剩余只读文件13个，位于 C:\Users\steph\AppData\Local\Temp\cleo-uiux-fixes-20260916\permissions-completion\desktop-final（10个 Git 测试对象）及同层 native-sandbox-06b1dff896214c38b07e43fea84ba444（3个初次原生客户端自动同步插件时生成的 Git pack 文件）。连同前述75个共88个，仍须完成最终清理；未尝试绕过此前强制删除的审批限制。

本文件是这次修复的交付/验证记录，不修改或替代仓库指导规则。
