# UI/UX 与相关 issues 修复进度

范围：完成 UI_UX_AUDIT_v0.4.8.md 中 U01–U23，并处理已识别的 UI/UX issues #29、#30、#32、#33、#34、#37、#38、#52、#57、#58。#10 只有标题，需要核对 v0.4.8 的同会话模型切换与其原意；分支申请和 #11 不作为界面缺陷处理。

完成条件：每项有实际实现与对应验证；全套检查、深浅主题、窄窗口、键盘/中文输入、失败恢复和必要后端契约通过；临时文件清理；最终重审；不能仅凭测试通过宣称所有问题完成。

## 基线

- 分支：codex/uiux-issues-v0.4.8。
- 0666b42：将 v0.4.8 产品与测试变化整合到 main 的历史上，保留 main 文档、AGENTS.md 和 data 示例。尚未推送或改变远端 main。
- 审查报告仍记录修改前状态，以固定 v0.4.8 源码链接作为基线。
- 视觉方向：克制、正文优先、简洁可读；内容顺序为工作内容、当前状态、必要操作、按需详情。动效只用于状态变化、展开收起和焦点反馈。

## 状态

| 范围 | 状态 | 验证/剩余要求 |
| --- | --- | --- |
| U01 / #37 焦点与快捷键 | 已实现首批，待整体视觉验收 | native modal、Tab 循环、嵌套 Escape、屏蔽底层审批与全局快捷键；新增浏览器回归通过 |
| U02 草稿 | 已实现首批 | 指令与连接向导切页保留；指令关闭重开不被后台读取覆盖；后续补模型设置整体流程验证 |
| U03 失败恢复 | 已实现首批 | 模型/指令读取错误与重试、任务恢复错误、启动重试；后续补请求竞态验证 |
| U13 / #29 记忆刷新 | 已实现，待最终综合验证 | 独立 load_memory 接口；进入页面、窗口返回、可见页定时、回合完成与整理结束刷新；合并并发请求、忽略旧响应；失败保留数据且可重试 |
| U04–U07 / #52 / #58 | 待实现 | 自动历史加载、阅读位置、长内容、统一运行状态与记录样式 |
| U08–U10 / U14–U16 / U23 | 待实现 | 字体、文案、记忆布局、真实状态、去除空操作入口 |
| U11–U12 / #32 | 待实现 | 自动客户端/登录检测、模型连接、macOS 路径发现、简化单选 |
| U17–U21 | 待实现 | 简化进化与验收；贡献、更新、发布中的自动读取/检查 |
| U22 / #33 Steer | 待实现 | 原生能力与降级、请求去重、投递状态、持久化、取消竞态 |
| #57 并行任务 | 待实现 | 前后端每任务运行隔离、审批、取消、切换与恢复 |
| #34 / #38 | 待实现 | 真实审批策略/访问模式、能力暴露、保存恢复、运行中生效规则 |
| #30 耗时 | 待实现 | 普通回复及 DreamAgent 实际阶段计时、失败/重试/恢复、持久化与显示 |

## 已运行验证

- 新增 ui/tests/uiux-regressions.smoke.mjs：先复现旧版焦点、审批、草稿、无错误反馈及记忆刷新失败；修复后十个场景通过。
- tests/desktop/test_service.py：31 项通过，包含普通对话/开发空间的记忆新修订、忽略后重新出现、保持当前任务。
- timeline-cache、model-connection-scope、harness-switch-ipc：14 项通过。
- npm run typecheck --prefix ui、Vite production build、修改 Python 文件的 Ruff 检查通过；构建仍有已有的大 chunk 提示。
- 最终全套测试、lint、production build 与整体视觉验证尚未完成。

## 临时资源

统一目录：C:\Users\steph\AppData\Local\Temp\cleo-uiux-fixes-20260916。基线导入 patch 已删除；测试将子目录放在此处并通过 finally 清理。最终完成前须删除整个任务临时目录并复核 git status。

本文件是这次修复的交付/验证记录，不修改或替代仓库指导规则。
